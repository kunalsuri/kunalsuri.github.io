# "What Is …" — Ideas

Just ideas. One line each. Nothing here tracks state — for that, run
`/what-is` with no argument, which reads the posts on disk. Two lists would
only drift apart.

Delete a line when it ships, or leave it; nobody is counting.

### Foundations
- What is an LLM?
- What is a token? — why billing, context limits and "it can't count letters" all trace to one idea
- What is a context window? — and why bigger is not straightforwardly better
- What is an embedding? — meaning as coordinates
- What is fine-tuning? — and when it is the wrong tool
- What is RAG? — retrieval as a substitute for memory
- What is inference vs. training? — the cost asymmetry that shapes the industry
- What is temperature? — the one sampling knob everyone touches blind

### Agents & tooling
- What is the difference between an Agent Skill and a Command in Claude Code? — lead with the contrast table
- What is an AI agent? — the marketing definition vs. the mechanical one
- What is MCP? — why a protocol was needed at all
- What is tool use / function calling? — the mechanism under every agent demo
- What is a system prompt? — and why it is not a config file
- What is an eval? — what separates a demo from a product
- What is context engineering?

### Systems & web
- What is a static site generator? — pairs with the Astro post
- What is island architecture? — Astro's core bet
- What is a CDN?
- What is server-side rendering? — the term that means four different things

### Working & leadership
- What is technical debt? — the metaphor misused in every planning meeting
- What is a platform team?
- What is a design doc? — and what makes one worth writing
